option_settings:
  aws:elasticbeanstalk:application:environment:
    PYTHONPATH: "/var/app/current/djangotutorial:$PYTHONPATH"
    DJANGO_SETTINGS_MODULE: "mysite.settings"
    # Set SECRET_KEY, DEBUG, and ALLOWED_HOSTS in the EB console if preferred.
    # DEBUG: "False"
    # ALLOWED_HOSTS: "djangotutorial-env.us-east-1.elasticbeanstalk.com,.elasticbeanstalk.com,localhost,127.0.0.1"

option_settings:
  aws:elasticbeanstalk:application:environment:
    PYTHONPATH: "/var/app/current/djangotutorial:$PYTHONPATH"
    DJANGO_SETTINGS_MODULE: "mysite.settings"
    # It is recommended to set SECRET_KEY in the EB console and not commit it.
    # DEBUG: "False"
    # ALLOWED_HOSTS: "djangotutorial-env.us-east-1.elasticbeanstalk.com,.elasticbeanstalk.com,localhost,127.0.0.1"


